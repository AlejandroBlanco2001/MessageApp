import { Box } from "@chakra-ui/react";

const ChatDisplay = ({}) => {
    return (
        <Box bg="yellow" height="70%" width="50%">
            <p>Chat Display</p>
        </Box>
    );
};

export default ChatDisplay;
